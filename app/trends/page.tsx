"use client";
import { Screen } from "../components/Screen";
export default function TrendsPage() {
  return (
    <Screen title="Trends">
      <div className="card nudge">📈 Trends screen coming next — weight, readiness, VO₂max over time.</div>
    </Screen>
  );
}
