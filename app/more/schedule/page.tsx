"use client";
import { Screen } from "../../components/Screen";
export default function SchedulePage() {
  return (
    <Screen title="Schedule" back="/more">
      <div className="card nudge">📅 Training schedule & calendar sync coming next — your weekly plan and race calendar in one place.</div>
    </Screen>
  );
}
